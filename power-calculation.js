//************ SECOND EXERCICE FUNCTION **************//

// const powerCalculation=(number1, number2)=>{
//     console.log(number1**number1)
// }
// powerCalculation(2, 3);

//************ CORRECTION **************//

const power = (number1, number2) => {
    const result = number1 ** number2;
    console.log(result);
  };
  
  power(2, 3); // 8

  const powerCalculation = (number1, number2) => {
    const result = number1 ** number2;
    console.log(result);
  };

  powerCalculation(5, 5); // 3125